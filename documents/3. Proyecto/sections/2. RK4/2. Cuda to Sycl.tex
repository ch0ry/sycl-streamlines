\subsection{Migración del código de CUDA}

\lstset{
	language=C++,
	basicstyle=\ttfamily\small,
	keywordstyle=\color{blue},
	commentstyle=\color{green!60!black},
	stringstyle=\color{red},
	numbers=left,
	numberstyle=\tiny,
	stepnumber=1,
	numbersep=5pt,
	breaklines=true,
	frame=single,
	tabsize=2
}

Con el fin de comparar los estándares de portabilidad,
especialmente si se parte de CUDA, es menester incluir el proceso de migración de una base de código en CUDA. Algunos estándares como el de OneAPI de Intel o HIP de AMD, proveen una herramienta que se encarga de migrar el código en CUDA hacia sus respectivos estándares. En el caso de OneAPI, esta se llama \textit{dpct} (Intel DPC++ Compatibility Tool) y para HIP, se llama \textit{HIPify}.

% Cabe destacar que ambas implementaciones no producen un código en SYCL o un SYCL nativo, HIPify produce código en su estándar homónimo HIP, y oneAPI produce una versión de SYCL que usa la librería de dpct, lo cual presenta un problema si no se desea compilar con el compilador de oneAPI.

Para ilustrar la diferencia entre la sintaxis de estos estándares, a continuación un fragmento del código de la función main en su versión de CUDA, DPCT y SYCL nativo:

\textbf{Versión del fragmento en CUDA:}

\begin{lstlisting}
  thrust::device_vector<integrator_rk4> dintg(num_seeds); 

  thrust::tabulate(
      dintg.begin(), dintg.end(), seed_generator{num_seeds}); 

  houti = thrust::copy(dintg.begin(), dintg.end(), houti); 
\end{lstlisting}

\textbf{Versión del fragmento en HIP:}

\begin{lstlisting}
  thrust::device_vector<integrator_rk4> dintg(num_seeds); 

  thrust::tabulate(
      dintg.begin(), dintg.end(), seed_generator{num_seeds}); 

  houti = thrust::copy(dintg.begin(), dintg.end(), houti); 
\end{lstlisting}

\textbf{Versión del fragmento en SYCL*:}

\begin{lstlisting}
  dpct::device_vector<integrator_rk4> dintg(num_seeds); 

  dpct::for_each_index(
      oneapi::dpl::execution::make_device_policy(q_ct1), dintg.begin(),
      dintg.end(), seed_generator{num_seeds}); 

  houti = std::copy(oneapi::dpl::execution::make_device_policy(q_ct1),
                    dintg.begin(), dintg.end(), houti); 
\end{lstlisting}

\textbf{Versión del fragmento en SYCL nativo:}

\begin{lstlisting}
  integrator_rk4* dintg = sycl::malloc_device<integrator_rk4>(num_seeds, q_ct1);

  seed_generator gen{static_cast<unsigned>(num_seeds)};

  q_ct1.submit([&](sycl::handler& h){
    h.parallel_for(sycl::range<1>(num_seeds), [=](sycl::id<1> idx) {
        const unsigned i = static_cast<unsigned>(idx[0]);
        dintg[i] = gen(i);
    });
  }).wait();

  q_ct1.memcpy(houtput.data() + (0) * num_seeds, dintg, num_seeds * sizeof(integrator_rk4)).wait();
\end{lstlisting}


Usando solamente este fragmento, es posible afirmar que la sintaxis entre CUDA y HIP no es muy diferente. Esto explica también por squé HIPify fue mucho más efectivo durante su migración automatizada, requiriendo cambios mínimos a la base de código.

En adición, en este caso en particular, donde se usa una tarjeta gráfica de marca AMD para el desarrollo, es requerido una configuración adicional para poder ejecutar el código usando la GPU con el compilador de oneAPI\footnote{Como se indica en las notas de la nueva versión del compilador de oneAPI \cite{intel_technologies_intel_2026} } (icpx), y durante una prueba preliminar, presentó

Esto presentó inconvenientes durante la migración y creó la necesidad de incluir a oneAPI como un estándar de portabilidad más a comparar en vez de tomarlo como un sabor diferente de SYCL.

Cabe destacar que ninguna de estas herramientas de migración pudieron satisfactoriamente migrar algunas llamadas de funciones exclusivas de CUDA, en particular \textit{Tex3D.get()}. Esta función se encarga de realizar la interpolación trilmineal de una textura, que es la función más matemáticamente demandante del algoritmo. A raíz de esto, se modificó el código original para que no se use la configuración y función exclusiva de CUDA. No obstante, esta acomodación es un punto cualitativo de comparación a favor de CUDA.


% En la tabla \ref{tab:criterios}, se presentan los criterios usados para la selección, con su respectiva explicación y valor considerado para la selección.

